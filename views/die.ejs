<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Hahmlet&family=Noto+Sans+KR&display=swap" rel="stylesheet">
    <style>
        html, body {
            font-family: 'Hahmlet', serif;
            font-family: 'Noto Sans KR', sans-serif;
            width: 100%;
            height: 100%;
            padding: 0;
            margin: 0;
        }
        .container{
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        .item{
            width: 20em;
            height: 15em;
            text-align: center;
            font-size: 3em;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-evenly;
        }
        .item1{
            width: 10em;
            height: 9.5em;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        .item2{
            width: 8em;
            height: 3em;
        }
        .input{
            width: 6em;
            height: 4em;
            display: flex;
            align-items: center;
            flex-wrap: wrap;
            justify-content: center;
        }
        input[type=button]{
            font-family: 'Hahmlet', serif;
            font-family: 'Noto Sans KR', sans-serif;
            font-size: 1em;
            width: 5em;
            height: 1.7em;
            text-align: center;
            border-radius: 20px;
            border: none;
        }
        input[type=button]:hover{
            font-family: 'Hahmlet', serif;
            font-family: 'Noto Sans KR', sans-serif;
            font-size: 1em;
            width: 5em;
            height: 1.7em;
            text-align: center;
            border-radius: 20px;
            border: none;
            box-shadow: 5px 5px 5px gray;
        }
    </style>
    <script>
        function mainpage(){
            location.href = "/main";
        }
        function menupage(){
            location.href = "/menu";
        }
    </script>
    <title>Tank Game</title>
</head>
<body>
    <div class="container">
        <div class="item">
            <div class="item1">
                <h1>You Die!</h1>
                <div class="input">
                    <input type="button" value="다시하기" onclick="mainpage()"><br>
                    <input type="button" value="메뉴" onclick="menupage()">
                </div>
            </div>
            <div class="item2">
                KillUser : <%= killuser %> <br>
                Kill : <%= kill %> <br>
                Time : <%= parseInt((time/3600)%100) %>:<%= parseInt(time/60) %>:<%= parseInt(time%60) %> <br>
            </div>
        </div>
    </div>
</body>
</html>